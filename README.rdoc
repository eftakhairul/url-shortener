== URL Shortener

This is very simple URL Shortener App. Here, I played with NoSQL: MongoDB
with Rails 4 and dvevice gem.

How to Install?

* Just clone the project

* Run the command: bundle install

* Change the DB configuration at config/mongoid.yml

It done. Just start server. Type: rails s


